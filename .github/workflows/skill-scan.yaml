name: Skill Scanner
run-name: "Skill Scanner (by @${{ github.actor }} via ${{ github.event_name }})"
on:
  workflow_call:
    inputs:
      severity:
        description: "SkillSpector severity level (CRITICAL, HIGH, MEDIUM, LOW)"
        required: false
        type: string
        default: "HIGH"
      fail_on_findings:
        description: "Fail the workflow when findings are detected"
        required: false
        type: boolean
        default: true
  workflow_dispatch:
    inputs:
      severity:
        description: "SkillSpector severity level (CRITICAL, HIGH, MEDIUM, LOW)"
        required: false
        default: "HIGH"
        type: choice
        options:
          - CRITICAL
          - HIGH
          - MEDIUM
          - LOW
      fail_on_findings:
        description: "Fail the workflow when findings are detected"
        required: false
        default: true
        type: boolean

permissions: {}

jobs:
  discover-skills:
    name: Discover skill paths
    runs-on: dlstreamer
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.find-paths.outputs.matrix }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 #7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Find all .github/skills directories
        id: find-paths
        run: |
          paths=$(find . -path '*/.github/skills' -type d \
            | sed 's|^\./||' \
            | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "matrix=$paths" >> "$GITHUB_OUTPUT"

  skillspector-scan:
    name: SkillSpector security scan (${{ matrix.skills-path }})
    needs: discover-skills
    runs-on: dlstreamer
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        skills-path: ${{ fromJson(needs.discover-skills.outputs.matrix) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 #7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Run SkillSpector scan
        uses: open-edge-platform/geti-ci/actions/skill-scan@0745e2b1612e4c25c3e83eb37bddeba8743a2439 # skill-scan/v0.1.1
        with:
          skills-path: ${{ matrix.skills-path }}
          severity-level: ${{ inputs.severity || 'HIGH' }}
          fail-on-findings: ${{ inputs.fail_on_findings || 'true' }}

  skill-validator-scan:
    name: skill-validator scan (${{ matrix.skills-path }})
    needs: discover-skills
    runs-on: dlstreamer
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        skills-path: ${{ fromJson(needs.discover-skills.outputs.matrix) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 #7.0.1
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Run skill-validator
        uses: open-edge-platform/geti-ci/actions/skill-validator@c64543e05e9f84c7c4ef212666c3ea5fc869e55a # skill-validator/v0.2.0
        with:
          skills-path: ${{ matrix.skills-path }}
          fail-on-findings: ${{ inputs.fail_on_findings || 'true' }}
          allow-dirs: "evals,example-prompts,benchmark"
